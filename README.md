# dotfiles

```
cd /tmp
git clone https://github.com/Connor-McCartney/dotfiles
cp -r dotfiles/.tex-templates ~/.tex-templates
cp -r dotfiles/.local/share/konsole ~/.local/share/konsole
cp -r dotfiles/.config/nvim ~/.config/nvim
cp -r dotfiles/.bashrc.d ~/.bashrc.d
rm -rf dotfiles
```
